# MeshVTON v2 — an environment COMPLETELY independent of v1 (never mix it with the v1 pins).
# Python >= 3.10. On Colab: pip install -r v2/requirements.txt
torch>=2.4
torchvision
diffusers>=0.34
transformers>=4.46
peft>=0.14
accelerate>=1.2
safetensors
sentencepiece
protobuf
numpy
opencv-python
Pillow
scikit-image>=0.22
lpips
einops
pyyaml
tqdm
pytest

# --- 3D: Phase 1 (lightweight, no compilation) ---
trimesh>=4
pyrender          # the appearance-ref render backend (EGL headless; pytorch3d NOT required)

# --- 3D: Phase 2+ (requires CUDA, installed separately) ---
# pip install "git+https://github.com/facebookresearch/pytorch3d.git"   # ~20-40 min compile
# pip install smplx>=0.1.28
# pip install "git+https://github.com/shubham-goel/4D-Humans.git"   # HMR2
